@using WebApplication1.Models;
@using PagedList.Mvc;
@model PagedList.PagedList<LoaiGiay>
@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Admin/Views/Shared/View1.cshtml";
}

<h2>Quản lý Loại giày</h2>
<div class="page-wrapper">
    <table width="100" class="table table-striped table-bordered table-hover" id="dataTables-example">
        <thead>
            <tr>
                <th>
                    Mã Loại
                </th>
                <th>
                    Tên Loại
                </th>
                <th></th>

            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr class="odd gradeX">

                    <td>
                        @item.MaLoai
                    </td>
                    <td>

                        @item.TenLoai
                    </td>

                    <td>

                        <a href="@Url.Action("Editloai", new { id = int.Parse(item.MaLoai) })" class="btn btn-primary">
                            Sửa
                        </a>
                        <a href="@Url.Action("Deleteloai", new { id = int.Parse(item.MaLoai) })" class="btn btn-danger">
                            Xóa
                        </a>
                    </td>
                </tr>
            }
    </table>
    <a href="@Url.Action("Createloai", "LoaiGiay") " class="btn btn-success">
        Thêm Loại giày
    </a>


</div>
<footer>
    <div class="MenuTrang" style="text-align:center;">
        trang @(Model.PageCount < Model.PageNumber ? 0 : Model.PageNumber)/@Model.PageCount
        @Html.PagedListPager(Model, page => Url.Action
                      ("Index", new { page = page })) <br />
    </div>
    <style>
        .MenuTrang li {
            display: inline;
        }
    </style>

</footer>

